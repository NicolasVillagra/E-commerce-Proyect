import { Router } from 'express';
//import { HANDLERS } from '../Handlers/userHandler';


const userRouter = Router();

// userRouter.get('/', HANDLER);
// userRouter.post('/login', HANDLER);
// userRouter.get('/:id', HANDLER);
// userRouter.delete('/:id', HANDLER);
// userRouter.delete('/disable/:id', HANDLER);
// userRouter.post('/', HANDLER);
// userRouter.put('/:id', HANDLER);


export default userRouter;